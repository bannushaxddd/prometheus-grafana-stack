# Node
app/node_modules/
npm-debug.log*

# Local Prometheus data (if run outside Docker)
data/
*.wal

# Official Prometheus binaries (download from the prometheus GitHub releases)
*.exe

# Env / secrets (keep .env.example tracked)
.env.local
# Uncomment to stop committing local passwords:
# .env

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log
